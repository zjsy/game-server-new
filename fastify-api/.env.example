# 开发环境配置 - 容器专用
NODE_ENV=development

# 数据库配置 - 容器网络地址
RDS_HOST_WRITE=127.0.0.1
RDS_HOST_READ=127.0.0.1
DB_PORT=3306
DB_DATABASE=fggame
DB_USERNAME=root
DB_PASSWORD=root
DB_PREFIX=

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=sy1314
REDIS_PORT=6379
REDIS_DB=0
REDIS_PREFIX=FG:


# Server
FASTIFY_CLOSE_GRACE_DELAY=1000
LOG_LEVEL=info

# Centrifugo 配置
CENTRIFUGO_API_URL=http://localhost:8000/api     # 宿主机访问
CENTRIFUGO_WS_URL=ws://localhost:8000/connection/websocket
CENTRIFUGO_API_KEY=my_api_key

# JWT 鉴权配置
# JWT 密钥 - 生产环境请使用强随机字符串
# 生成命令: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change_this_to_a_secure_secret_in_production_minimum_32_characters
JWT_EXPIRES_IN=24h

# AES 加密配置（已弃用，保留用于兼容）
# 生成命令: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AES_SECRET_KEY=17c9ee7fcdeec4edfcaa19fde256c07267907f3dda4b5351176e9e5ac8dc31a1
AES_ALLOW_PLAIN=true
